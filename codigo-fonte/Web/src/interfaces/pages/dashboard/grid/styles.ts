import { SxProps } from '@mui/material';

export const styles = {
	tilesContainer: {
		width: '100%',
		display: 'flex',
		flexWrap: 'wrap',
		justifyContent: 'space-around',
		alignItems: 'center',
		mb: 10,
	},
} satisfies Record<string, SxProps>;
