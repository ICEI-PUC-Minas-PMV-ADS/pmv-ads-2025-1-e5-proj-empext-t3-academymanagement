import { Breakpoint } from '@mui/material/styles';

export type IReturnType = boolean;

export type IQuery = 'up' | 'down' | 'between' | 'only';

export type IValue = Breakpoint | number;

export type IBreakpointOrNull = Breakpoint | null;
